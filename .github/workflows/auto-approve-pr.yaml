name: Auto Approve
on:
  pull_request:
    types: [ready_for_review]
jobs:
  auto-approve:
    uses: brevisdev/.github-private/.github/workflows/pr-auto-approve.yaml@main
    secrets:
      PR_REVIEW_PAT: ${{ secrets.PR_REVIEW_PAT }}